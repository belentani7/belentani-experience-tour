// Tailwind CSS v4 is wired up through the `@tailwindcss/vite` plugin, so no
// PostCSS plugins are required here. This file exists to keep the build
// self-contained and to stop Vite from inheriting a parent-directory
// PostCSS config (e.g. a stray `postcss.config.mjs` in the parent folder).
export default {
  plugins: [],
};
